{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ServerNotification",
  "description": "Everything the Core may push to an attached Client unprompted.",
  "oneOf": [
    {
      "description": "The Core's coarse state changed (idle ↔ recording).",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "core/stateChanged"
        },
        "params": {
          "$ref": "#/$defs/CoreStateChangedParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "A Meeting was created, updated, or removed. Clients refresh from it\nrather than polling.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "meeting/changed"
        },
        "params": {
          "$ref": "#/$defs/MeetingChangedParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "A new Transcript segment. Delivered only to subscribed connections,\nand delivered lossily: a Client that falls behind loses captions\nrather than slowing capture or being disconnected (ADR-0028).",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "transcript/segmentAdded"
        },
        "params": {
          "$ref": "#/$defs/TranscriptSegmentAddedParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "Some captions were dropped because this connection was too slow.\nSent so a Client can show a gap rather than silently missing words.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "transcript/captionsDropped"
        },
        "params": {
          "$ref": "#/$defs/TranscriptCaptionsDroppedParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "A Speaker was created, renamed, or had its Voiceprint deleted. A\nrename is retroactive across every Meeting, so a Client showing any\ntranscript needs to know rather than poll.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "speaker/changed"
        },
        "params": {
          "$ref": "#/$defs/SpeakerChangedParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "Diarization progressed, finished, or stopped. Post-meeting work the\nOperator did not ask for out loud still has to be visible while it\nruns.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "diarize/progress"
        },
        "params": {
          "$ref": "#/$defs/DiarizeProgressParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    },
    {
      "description": "A model download progressed, finished, or stopped.\n\nMirrors `diarize/progress`, and for the same reason: work the Core\ndoes on its own behalf has to be visible, or an Operator cannot tell\nit from the product misbehaving. That matters more here than for\nDiarization — a fresh install fetches gigabytes without being asked.",
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "const": "models/progress"
        },
        "params": {
          "$ref": "#/$defs/ModelProgressParams"
        }
      },
      "required": [
        "method",
        "params"
      ]
    }
  ],
  "$defs": {
    "Attribution": {
      "description": "Why a segment is attributed to whom it is.\n\nADR-0008 counts visible match attribution among the legibility surfaces\nit made mandatory in exchange for storing biometrics. An Operator who\ncannot ask why has no way to know whether to correct it.",
      "oneOf": [
        {
          "description": "Matched an existing Voiceprint.",
          "type": "string",
          "const": "voiceprint"
        },
        {
          "description": "Clustered within this Meeting and matched nobody in History.",
          "type": "string",
          "const": "clustered"
        },
        {
          "description": "The mic-channel prior did the work (ADR-0029 as amended).",
          "type": "string",
          "const": "channel"
        },
        {
          "description": "The Operator said so, and that outranks everything above.",
          "type": "string",
          "const": "operator"
        }
      ]
    },
    "AudioChannel": {
      "description": "Which capture leg a segment came from. The mic channel is where the\nOperator is (ADR-0029 as amended).",
      "type": "string",
      "enum": [
        "mic",
        "system"
      ]
    },
    "CoreState": {
      "description": "The Core's coarse, observable state.",
      "type": "string",
      "enum": [
        "idle",
        "recording"
      ]
    },
    "CoreStateChangedParams": {
      "type": "object",
      "properties": {
        "state": {
          "$ref": "#/$defs/CoreState"
        }
      },
      "required": [
        "state"
      ]
    },
    "DiarizeProgressParams": {
      "type": "object",
      "properties": {
        "doneMs": {
          "type": "integer",
          "format": "int64"
        },
        "meetingId": {
          "type": "string"
        },
        "state": {
          "$ref": "#/$defs/DiarizeState"
        },
        "totalMs": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "meetingId",
        "state",
        "doneMs",
        "totalMs"
      ]
    },
    "DiarizeState": {
      "description": "What a diarization job is doing.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "idle",
            "running"
          ]
        },
        {
          "description": "Ran, and could not: a missing or unloadable model. The Transcript\nstays unattributed and says so rather than the Meeting failing.",
          "type": "string",
          "const": "unavailable"
        }
      ]
    },
    "Meeting": {
      "description": "One recorded session: the unit of storage, retrieval, and summary.",
      "type": "object",
      "properties": {
        "audioNotes": {
          "description": "What this recording lost, in the Operator's terms — a capture leg that\nnever started, one that died partway, audio that would not encode.\n\nEmpty for a whole recording. This is in the record rather than only in\na log because a Meeting missing half its audio is otherwise\nindistinguishable from one where nobody spoke, and the Operator finds\nout by reading a transcript that makes no sense.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "audioPath": {
          "type": [
            "string",
            "null"
          ]
        },
        "calendarAttendees": {
          "description": "Who the event invited. **Stored, not applied**: these become\nSpeaker-naming suggestions in M3, and turning an invitation into an\nattribution before Diarization exists would be inventing who spoke.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "calendarEventId": {
          "description": "The calendar event this Meeting was armed from, when one named it\n(ADR-0036). Kept so the record can say *which* scheduled meeting\nthis was, not merely that it had a title.",
          "type": [
            "string",
            "null"
          ]
        },
        "detectedApp": {
          "description": "The app detection attributed this Meeting to; the Mirror's slug\nbefore a Title exists.",
          "type": [
            "string",
            "null"
          ]
        },
        "durationSeconds": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "endedAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "UUIDv7. Its first 8 hex characters appear in the Mirror's filename\nand are the stable handle outside references should key on.",
          "type": "string"
        },
        "mirrorFilename": {
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "description": "The Operator's own writing (ADR-0018). **The one mutable thing here**\n— the Transcript and its attribution are what happened and never\nchange, while Notes are what the Operator chose to write and stay\neditable forever.",
          "type": [
            "string",
            "null"
          ]
        },
        "startedAt": {
          "type": "string"
        },
        "summary": {
          "description": "The generated Summary, as markdown. Derived rather than observed, so\nregenerating it destroys nothing.",
          "type": [
            "string",
            "null"
          ]
        },
        "summaryBackend": {
          "description": "Which Backend produced the Summary. An Operator who chose Cloud and\ngot local quality is owed the reason (story 38); one who chose Local\nis owed evidence that is what ran.",
          "type": [
            "string",
            "null"
          ]
        },
        "summaryGaps": {
          "description": "What this Summary could not cover, when chunks of the Meeting failed.\nAdditive and optional (ADR-0028): a Client that predates it sees a\nMeeting exactly as before.",
          "type": [
            "string",
            "null"
          ]
        },
        "summaryGeneratedAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "description": "None until titled. Clients render the fallback rather than inventing\na title, so \"untitled\" never becomes a stored string.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "startedAt"
      ]
    },
    "MeetingChangeKind": {
      "description": "What happened to a Meeting.",
      "type": "string",
      "enum": [
        "started",
        "updated",
        "stopped",
        "deleted"
      ]
    },
    "MeetingChangedParams": {
      "type": "object",
      "properties": {
        "kind": {
          "$ref": "#/$defs/MeetingChangeKind"
        },
        "meeting": {
          "description": "Absent for a deletion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Meeting"
            },
            {
              "type": "null"
            }
          ]
        },
        "meetingId": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "meetingId"
      ]
    },
    "ModelProgressParams": {
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string"
        },
        "doneBytes": {
          "type": "integer",
          "format": "int64"
        },
        "key": {
          "description": "Registry key of the model being fetched.",
          "type": "string"
        },
        "stopped": {
          "description": "Set when the fetch ended without finishing — cancelled, or failed.\nA download that stops is not the same as one still running at the\nsame byte count, and a Client showing a frozen bar cannot tell them\napart.",
          "type": [
            "string",
            "null"
          ]
        },
        "totalBytes": {
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "key",
        "displayName",
        "doneBytes",
        "totalBytes"
      ]
    },
    "Speaker": {
      "description": "A Speaker, as the Voice Registry shows it.",
      "type": "object",
      "properties": {
        "confirmed": {
          "description": "Set when the Operator named this Speaker: naming is confirmation\n(ADR-0008 as amended), and a confirmed Voiceprint outranks an\nunconfirmed one when matching.",
          "type": "boolean"
        },
        "createdAt": {
          "type": "string"
        },
        "displayName": {
          "description": "Absent until the Operator names it. Clients render a numbered\npseudonym (\"Speaker 3\"); it is deliberately not stored, because a\nstored pseudonym looks like a name someone chose.",
          "type": [
            "string",
            "null"
          ]
        },
        "firstSeenAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "hasVoiceprint": {
          "description": "Whether the app can still recognize this voice. False after a\nVoiceprint deletion, with the Speaker and the record intact.",
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "isOperator": {
          "description": "The Operator's own Speaker, displayed \"You\" (ADR-0029 as amended).",
          "type": "boolean"
        },
        "meetingsSeenIn": {
          "type": "integer",
          "format": "int64"
        },
        "voiceprintModel": {
          "description": "Which model produced the stored Voiceprint. Present so a Registry can\nexplain why an upgrade re-embedded, rather than recognition silently\nchanging quality.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "isOperator",
        "hasVoiceprint",
        "confirmed",
        "meetingsSeenIn",
        "createdAt"
      ]
    },
    "SpeakerChangedParams": {
      "type": "object",
      "properties": {
        "speaker": {
          "$ref": "#/$defs/Speaker"
        }
      },
      "required": [
        "speaker"
      ]
    },
    "TranscriptCaptionsDroppedParams": {
      "type": "object",
      "properties": {
        "dropped": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "meetingId": {
          "type": "string"
        }
      },
      "required": [
        "meetingId",
        "dropped"
      ]
    },
    "TranscriptSegment": {
      "description": "One attributed span of the Transcript.",
      "type": "object",
      "properties": {
        "attribution": {
          "description": "Why `speaker_id` is what it is (ADR-0008's visible match\nattribution). Absent on segments written before Diarization ran.",
          "anyOf": [
            {
              "$ref": "#/$defs/Attribution"
            },
            {
              "type": "null"
            }
          ]
        },
        "channel": {
          "$ref": "#/$defs/AudioChannel"
        },
        "endMs": {
          "type": "integer",
          "format": "int64"
        },
        "id": {
          "type": "string"
        },
        "sequence": {
          "type": "integer",
          "format": "int64"
        },
        "speakerId": {
          "description": "Set by Diarization in M3. Until then the channel carries attribution.",
          "type": [
            "string",
            "null"
          ]
        },
        "startMs": {
          "type": "integer",
          "format": "int64"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "sequence",
        "channel",
        "startMs",
        "endMs",
        "text"
      ]
    },
    "TranscriptSegmentAddedParams": {
      "type": "object",
      "properties": {
        "meetingId": {
          "type": "string"
        },
        "segment": {
          "$ref": "#/$defs/TranscriptSegment"
        }
      },
      "required": [
        "meetingId",
        "segment"
      ]
    }
  }
}
