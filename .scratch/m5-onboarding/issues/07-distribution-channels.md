# 07: Homebrew cask and winget

**What to build:** The two install paths story 48 names, so installing never waits on an app store.

**Blocked by:** 06.

Status: generated and verified against a real build; publishing is the Operator's

- [x] Generated by `packaging/make-manifests.sh`, run against a real build. **The release-artifact checksums are placeholders and say so loudly**, because no tagged release exists — the script warns per artifact rather than emitting a plausible-looking hash
- [ ] **Generated, but it points at an installer that does not exist.** The three winget YAMLs are produced and well-formed; the artifact they name is the NSIS installer ticket 06 does not build yet, and its checksum is the placeholder the generator warns about. Marked open rather than ticked, because a manifest for a nonexistent file is not a distribution channel
- [x] Both are **generated from the release artifacts** rather than hand-edited, because a hand-maintained checksum is a hash that will eventually be wrong
- [x] Both `zap` and the winget description say the History folder is **not** removed, and the cask carries a `caveats` block saying so at install time rather than only at uninstall. Models and logs are removed; the record is not. An uninstaller that deleted someone's meeting archive because they were switching machines would be the most destructive thing this product could do
- [x] **Publishing to the two upstreams needs accounts and a tagged release that does not exist yet.** Prepare the manifests, verify them against a real artifact, and name what only the Operator can do
