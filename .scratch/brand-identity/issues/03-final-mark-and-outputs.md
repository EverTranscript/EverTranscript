# 03: The final mark and every output

**What to build:** From the chosen concept: `brand/src/mark.svg` and `mark-small.svg`, the tile compositions, the four tray states (ready / recording / busy / attention), the wordmark in an OFL typeface converted to outlines, light and dark lockups, and `brand/README.md` (identity, tokens, do/don't, the gitignore trap, how to rebuild). Then `pnpm -C brand render` and a visual review of every family, plus the macOS 26 Icon Composer package previewed with `ictool` and compiled with `actool`.

**Blocked by:** 02.

**Status:** resolved — B confirmed; all outputs rendered from the final masters
